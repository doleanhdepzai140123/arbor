use "./math.ab"

fn main() {
  println("add = {math.add(2, 3)}")
  println("mul = {math.mul(4, 5)}")

  var total = 0
  for i in 1..5 {
    total += math.mul(i, i)
  }
  println("sum of squares 1..4 = {total}")
}
