// Sample used by tests/self_hosted.mjs and the bootstrap pipeline —
// exercises the language subset that both the reference toolchain and
// the self-hosted compiler support.
// No /// doc comments, no *= /=, no hex ints, no float exponents,
// no string interpolation, no ! or &.

struct Point {
  x: Int
  y: Int
}

fn add(a: Int, b: Int) -> Int {
  a + b
}

fn classify(n: Int) -> Str {
  if n >= 10 {
    return "big"
  } else if n <= -5 {
    return "small"
  }
  "mid"
}

fn main() {
  let name = "world\ttabbed\n"
  var total = 0
  let xs = [3, 14, 15]
  for x in xs {
    total += x
  }
  let p = Point { x: 1, y: total * 2 }
  let ratio = 0.5
  let ok = total != 0 and not (ratio > 1.0)
  if ok {
    println("sum is")
    println(total)
    println(p.x)
    println(classify(total))
    println(name.len())
  } else {
    println("empty")
  }
}
